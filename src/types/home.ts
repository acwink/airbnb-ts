export interface IDestAddress {
  name: string;
  homes: [string, string, string, string, string, string];
}

export interface ILongforItem {
  city: string;
  price: string;
  picture_url: string;
  image_url: string;
}
